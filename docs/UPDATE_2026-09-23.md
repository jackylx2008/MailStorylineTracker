# 2026-09-23 项目更新

## 已完成

- 初始化 `main` Git 分支和项目结构。
- 接入经过验证的 126.com IMAP SSL、登录及客户端 `ID` 流程。
- 实现文件夹、From、To/Cc、主题/正文关键词和日期筛选。
- 实现 JSON 增量状态、EML/附件归档、结构化记录和邮件审核 HTML。
- 实现邮件会话归并、OpenAI 兼容 AI 事项梳理及时间线 HTML。
- 完成 Tkinter GUI、共享日志、进度、单任务约束和安全取消。
- 增加只验证认证、不访问邮件的 `check-login` 命令。

## 验证状态

- 8 个自动化测试通过。
- Python 完整编译、flake8 和 Git 补丁格式检查通过。
- Tkinter 隐藏窗口构建通过。
- 本机 126 IMAP TLS 服务可连接；当前真实账号在 `LOGIN` 阶段被服务器以账号或授权码错误拒绝。
- 两次真实登录尝试均未选择文件夹，未搜索、读取、下载或修改邮件。
- 本地 OpenAI 兼容服务可响应，但模型接口要求 API Key；需在本地配置后完成真实 AI 对话验证。

## 下一步

1. 在 126 邮箱设置中确认 IMAP 已开启，并重新核对当前账号对应的客户端授权码。
2. 使用 `python mail_storyline.py check-login` 完成纯登录验证。
3. 配置本地 `AI_API_KEY` 后运行 `python mail_storyline.py check-ai`。
4. 登录和 AI 检查通过后，再进行文件夹枚举、筛选预览及首次增量归档。

真实账号、授权码、API Key、邮件、附件、日志及处理结果只保存在本机忽略文件和数据目录中，不进入 Git。
